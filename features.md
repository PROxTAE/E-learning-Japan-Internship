# E-Learning Platform - Current Features

เอกสารนี้สรุปฟีเจอร์ทั้งหมดที่ระบบสามารถทำได้ในปัจจุบัน โดยแบ่งเป็นฝั่ง Frontend และ Backend

## 🖥️ Frontend (ส่วนหน้าบ้าน)

1. **ระบบ Teacher Dashboard (`/teacher/dashboard`)**
   - **หน้าต่างแสดงภาพรวม (Stats Overview):** สรุปจำนวนควิซทั้งหมด, ควิซที่เผยแพร่แล้ว (Published), แบบร่าง (Draft), ควิซที่เก็บเข้ากรุ (Archived), จำนวนครั้งที่ทำ (Total Attempts) และคะแนนเฉลี่ย
   - **การแสดงผลควิซ:** สามารถเลือกดูรายการควิซได้ทั้งแบบ Grid View และ Table View
   - **ระบบตัวกรองหมวดหมู่:** สามารถกรองรายการควิซตามหมวดหมู่ได้
   - **พาเนลรายละเอียด (Detail Panel):** เมื่อคลิกที่ควิซ จะมีหน้าต่างด้านข้างแสดงรายละเอียดของควิซนั้นๆ แบบรวดเร็ว
   - **ระบบแชร์ควิซ (Share Quiz Modal):** สามารถกดแชร์เพื่อดูรหัส (Join Code) และส่งให้นักเรียนเข้าทำควิซได้
   - **ระบบรองรับหลายภาษา (i18n):** มี Context รองรับการสลับภาษาบน UI (ไทย, อังกฤษ, ฯลฯ)

2. **ระบบ Quiz Builder (`/teacher/create-quiz`)**
   - หน้าสำหรับให้อาจารย์สร้างและแก้ไขข้อสอบ/ควิซ (สร้างคำถาม, กำหนดคำตอบ, ตั้งค่าควิซ)

3. **ระบบ Student Play (`/play/[code]` และ `/quiz`)**
   - **ระบบเข้าทำควิซด้วยรหัส:** นักเรียนสามารถเข้าทำควิซผ่าน URL ที่มี Join Code
   - หน้า UI สำหรับการตอบคำถามข้อสอบ

## ⚙️ Backend (ส่วนหลังบ้าน)

1. **ระบบจัดการควิซ (Quiz CRUD API)**
   - `GET /api/quizzes`: ดึงรายการควิซทั้งหมด
   - `GET /api/quizzes/:id`: ดึงข้อมูลควิซตาม ID
   - `POST /api/quizzes`: สร้างควิซใหม่
   - `PUT /api/quizzes/:id`: แก้ไขข้อมูลควิซทั้งหมด
   - `DELETE /api/quizzes/:id`: ลบควิซทิ้ง

2. **ระบบจัดการสถานะและการเข้าถึงควิซ**
   - `PATCH /api/quizzes/:id/status`: ปรับเปลี่ยนสถานะของควิซ (เช่น จาก Draft เป็น Published)
   - `POST /api/quizzes/:id/generate-code`: สุ่มสร้างรหัสเข้าร่วมควิซ (Join Code) เช่น รหัส 6 หลัก
   - *มีฟังก์ชันดึงข้อมูลควิซด้วย Join Code สำหรับฝั่งนักเรียน*

3. **ระบบจัดการไฟล์และรูปภาพ (Image Management)**
   - `POST /api/quizzes/images/upload`: อัปโหลดรูปภาพ (ใช้สำหรับใส่รูปประกอบในคำถาม/คำตอบ)
   - `DELETE /api/quizzes/images/:filename`: ลบรูปภาพเมื่อไม่ได้ใช้งาน

4. **ระบบฐานข้อมูลและโครงสร้าง (Infrastructure)**
   - จัดการฐานข้อมูลผ่าน `db.js` (คาดว่าเป็น SQLite หรือฐานข้อมูลที่เซ็ตอัพไว้)
   - มี Middleware สำหรับจัดการไฟล์อัปโหลดแบบ Multipart (`upload.middleware.js`)
